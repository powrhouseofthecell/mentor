const config = {
  preset: "ts-jest",
  testEnvironment: "node",
  verbose: false,
  // automock: true,
  // collectCoverage: true,
  // collectCoverageFrom: ["<rootDir>/src/app/**/*.ts"],
  // testMatch: ["<rootDir>/src/tests/sum/**/*.ts"], // this will only run test in the sum folder.
};

module.exports = config;
